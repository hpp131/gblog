syntax = "proto3";
option go_package = "github.com/hpp131/gblog/pb";

message PageRequest {
    // 分页大小, 一个多少个
    // page_size -> PageSize
    int64 page_size = 1;
    // 当前页, 查询哪一页的数据
    int64 page_number = 2;
}